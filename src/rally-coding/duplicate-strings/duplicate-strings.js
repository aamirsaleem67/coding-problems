const duplicateStrings = (strArg, k) => {
    const duplicateStrings = [];
    for (let ii = 0; ii < k; ii++) {
        duplicateStrings.push(strArg);
    }
    return duplicateStrings;
};

export default duplicateStrings;
